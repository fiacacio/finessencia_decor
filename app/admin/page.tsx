import { AdminDashboard } from '@/components/admin/admin-dashboard'

export const metadata = { title: 'Finessência | Administração' }

export default function AdminPage() {
  return <AdminDashboard />
}
